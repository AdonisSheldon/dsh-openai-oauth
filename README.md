# dsh-openai-codex-oauth

An unofficial, self-contained DeepSeek Harness plugin that exposes pi-ai's `openai-codex` models through ChatGPT account OAuth. The plugin preserves the Harness agent loop and supports an explicit choice between Browser login and Device Code login.

Development is in progress. Installation and release instructions will be added after the packed-plugin checks pass.
