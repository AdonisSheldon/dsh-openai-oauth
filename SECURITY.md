# Security policy

Do not report access tokens, refresh tokens, authorization codes, or credential files in public issues. Until a private security contact is published, report only a redacted reproduction in a GitHub issue and ask the maintainer for a private channel.

Version 0.1.0 supports only a local Web Host bound to loopback and a local headless CLI. Remote, proxied, container-forwarded, and Electron transports are not supported.
